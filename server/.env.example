PORT=3001
# Comma-separated list of allowed origins (no trailing slash).
# Supports full origins (https://example.com), hostnames (example.com), host:port (localhost:3000),
# wildcard suffixes (*.vercel.app), or "*" to allow all.
# Leave empty to allow all origins (dev-friendly; tighten for production).
CORS_ORIGIN=

# simulation | puppeteer
EXTRACTION_MODE=simulation

# Ingredient Harvester upstream (used by /api/ingredient-harvester proxy)
INGREDIENT_HARVESTER_BASE_URL=http://localhost:8008
INGREDIENT_HARVESTER_PROXY_TIMEOUT_MS=60000

# Ensure simulation returns at least this many variants (rows)
SIMULATION_TARGET_VARIANTS=850

# Puppeteer / real extraction knobs
# Limit how many product pages to process (applies to Shopify feed + sitemap discovery)
# Keep this low in production to avoid edge/proxy timeouts on long crawls.
MAX_PRODUCTS=6
PUPPETEER_CONCURRENCY=3
PUPPETEER_NAV_TIMEOUT_MS=8000
PUPPETEER_FETCH_TIMEOUT_MS=8000
PUPPETEER_LAUNCH_TIMEOUT_MS=15000
PUPPETEER_SCRAPE_TIMEOUT_MS=45000
PUPPETEER_USER_AGENT=
PUPPETEER_EXECUTABLE_PATH=

# Shopify feed paging protection
SHOPIFY_MAX_PAGES=20
LOW_STOCK_THRESHOLD=10
# Variant discovery: group "Title — Variant" single-default products into a single product with many variants.
# Values: off | auto | title
SHOPIFY_VARIANT_DISCOVERY=auto

# Sitemap discovery
MAX_SITEMAPS=4
