PORT=3001
# Comma-separated list of allowed origins (e.g. http://localhost:3000,http://localhost:3001)
# Leave empty to allow all origins (dev-friendly; tighten for production).
CORS_ORIGIN=

# simulation | puppeteer
EXTRACTION_MODE=simulation

# Ensure simulation returns at least this many variants (rows)
SIMULATION_TARGET_VARIANTS=850

# Puppeteer / real extraction knobs
# Limit how many product pages to process (applies to Shopify feed + sitemap discovery)
MAX_PRODUCTS=50
PUPPETEER_CONCURRENCY=2
PUPPETEER_NAV_TIMEOUT_MS=30000
PUPPETEER_USER_AGENT=

# Shopify feed paging protection
SHOPIFY_MAX_PAGES=20
LOW_STOCK_THRESHOLD=10

# Sitemap discovery
MAX_SITEMAPS=10
